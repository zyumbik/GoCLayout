<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/text_offset"
    android:paddingRight="@dimen/text_offset"
    android:paddingTop="@dimen/text_offset"
    android:paddingBottom="@dimen/text_offset" >

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_title"
        android:textColor="@color/text_regular"
        android:text="This is the title"/>

    <ImageButton
        android:id="@+id/arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:layout_centerHorizontal="true"
        android:background="@drawable/ic_keyboard_arrow_down_24dp"/>

    <TextView
        android:id="@+id/short_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_body"
        android:textColor="@color/text_secondary"
        android:layout_alignEnd="@id/arrow"
        android:layout_alignRight="@id/arrow"
        android:layout_below="@id/title"
        android:text="Something short about this menu that is nice"/>

    <TextView
        android:id="@+id/full_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_body"
        android:layout_below="@id/arrow"
        android:text="@string/lorem"/>

</RelativeLayout>